<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.kamil.aplikacja.PrimeNumberActivity"
    tools:showIn="@layout/activity_prime_number">

    <EditText
        android:id="@+id/primeNumberEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="12dp"
        android:ems="10"
        android:inputType="number"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginLeft="12dp" />

    <Button
        android:id="@+id/primeNumberBtn"
        android:layout_width="99dp"
        android:layout_height="53dp"
        android:layout_marginBottom="394dp"
        android:layout_marginTop="8dp"
        android:onClick="primeNumberClick"
        android:text="@string/start"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@+id/primeNumberEditText"
        app:layout_constraintTop_toBottomOf="@+id/primeNumberEditText" />

    <ProgressBar
        android:id="@+id/progressBarPrimeNumber"
        android:layout_width="0dp"
        android:layout_height="49dp"
        android:layout_marginTop="38dp"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/primeNumberBtn" />

</android.support.constraint.ConstraintLayout>
